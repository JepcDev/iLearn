// console.log('Hola mundo');

// DEV:TODO -> 2- Convertir de kilómetros a millas, sabiendo que: (1 kilómetro es igual a 0.62 millas )
// DEV:COMMENT -> Ejemplo: 10 kilómetros = 6.21 millas

// export let kmToMillas:number = 0.62;

// export let numConvertir: number = 10;

// export let conversion: number = numConvertir * kmToMillas;

// console.log(`${numConvertir} kilómetros = ${conversion} millas`);

// Otra forma
let kms = 10;
let miles = kms * 0.62;

console.log(`${kms} Kilómetros es igual a millas: ${miles}`);